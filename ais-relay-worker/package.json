{
  "name": "boatboard-ais-relay",
  "private": true,
  "description": "Cloudflare Worker WebSocket proxy for BoatBoard → AISStream",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
